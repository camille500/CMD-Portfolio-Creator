<%- include('../partials/navigation.ejs') %>

<h2 class="content-heading">Hi <%=user.data.name%></h2>
<p>Dit is de portfolio creator voor studenten van de opleiding CMD Amsterdam. <br/>
  Hieronder vindt je een overzicht met de stappen die je nog moet doorlopen.</p>

  <div class="row">
    <div class="col-xs-6 col-sm-4 col-lg-2">
      <a class="block block-link-hover2 text-center" href="javascript:void(0)">
        <div class="block-content block-content-full <%if(user.data.template == 0) { %>bg-danger<% } else { %>bg-success<% } %>">
          <i class="si <%if(user.data.template == 0) { %>si-close<% } else { %>si-check<% } %> fa-4x text-white"></i>
          <div class="font-w600 text-white-op push-15-t">Template</div>
        </div>
      </a>
    </div>
    <div class="col-xs-6 col-sm-4 col-lg-2">
      <a class="block block-link-hover2 text-center" href="javascript:void(0)">
        <div class="block-content block-content-full <%if(user.data.about == false) { %>bg-danger<% } else { %>bg-success<% } %>">
          <i class="si <%if(user.data.about == false) { %>si-close<% } else { %>si-check<% } %> fa-4x text-white"></i>
          <div class="font-w600 text-white-op push-15-t">Over jou</div>
        </div>
      </a>
    </div>
    <div class="col-xs-6 col-sm-4 col-lg-2">
      <a class="block block-link-hover2 text-center" href="javascript:void(0)">
        <div class="block-content block-content-full <%if(projects.length <= 0) { %>bg-danger<% } else { %>bg-success<% } %>">
          <i class="si <%if(projects.length <= 0) { %>si-close<% } else { %>si-check<% } %> fa-4x text-white"></i>
          <div class="font-w600 text-white-op push-15-t">Projecten</div>
        </div>
      </a>
    </div>
  </div>

<%- include('../partials/navigation-footer.ejs') %>
